# AI Revenue Recovery -- backend dependencies
# Deliberately minimal: no ORM, no settings lib, no SSE lib, no LLM SDK.
# Everything else is Python stdlib (sqlite3, hashlib, hmac, random, datetime).
# This keeps `pip install` fast and avoids wheel-availability surprises.

fastapi>=0.115
uvicorn>=0.32
httpx>=0.27
